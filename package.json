{
  "name": "retrofm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "make dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && DATABASE_URL=\"postgresql://retrowave:retrowave_pass@localhost:5432/retrowave\" npx tsx src/index.ts",
    "setup": "make setup",
    "seed": "cd backend && DATABASE_URL=\"postgresql://retrowave:retrowave_pass@localhost:5432/retrowave\" npx tsx src/seed.ts",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "lint": "cd frontend && npx eslint"
  }
}
